% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_tidy.R
\name{self_referencing}
\alias{self_referencing}
\title{self_referencing tag to allow self referencing in arguments}
\usage{
self_referencing()
}
\description{
Enhance functions like \code{transform}, \code{mutate}, \code{summarize} ... by allowing
arguments to reference themselves when they are formulas.
}
\details{
to detect rigorously the type of the argument it would be necessary to
evaluate it, which for these functions is not an option, therefore we
just check if the expressions starts with a \code{~} character.
}
\examples{
self_referencing$transform(head(iris,2), Petal.Width = ~1000*(.), Species = ~toupper(.))
\dontrun{
library(dplyr)
self_referencing$summarize(iris, Petal.Width = ~median(.), Sepal.Length = ~mean(.))
}
}
